const userModel = require("../models/userModel")
const assert = require('assert')

describe('#users.js', () => {
    describe('#users.login()',(done) => {
        it('',() => {
            assert.strictEqual(sum(1, 2), 3);
        })
    })    
})